{
  "name": "mirai-onnx-web",
  "version": "0.1.0-phase6",
  "description": "TypeScript preprocessing pipeline for the Mirai breast-cancer risk model, feeding image_encoder.onnx",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dicom-parser": "^1.8.21"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
